/// <reference types="@wdio/globals/types" />

export const clickOnButton = async (element: WebdriverIO.Element) => {
    await element.click();
};

export const clickOnCheckbox = async (element: WebdriverIO.Element) => {
    await element.click();
};

export const clickOnLink = async (element: WebdriverIO.Element) => {
    await element.click();
};